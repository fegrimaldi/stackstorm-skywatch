---
name: "get_solar"
pack: "skywatch"
action_ref: "skywatch.solar"
description: "Gets solar observation data for Alpharetta, GA."
formats:
  - display: get solar skywatch
    representation:
    - get solar skywatch {{location=Atlanta, GA, USA}}
ack:
  enabled: true
  format: "Executing `{{ actionalias.ref }}`, your shortened execution ID is `{{ execution.id[:2] }}..{{ execution.id[-2:] }}`"
  append_url: true
result:
  format: "Action completed! The Solar Observation is `{{ execution.result }}`."